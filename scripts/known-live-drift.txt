# Allowlist untuk `scripts/check-live-drift.sh` — slug migrasi yang ADA di
# ledger live `CDPS SG` tapi TIDAK punya berkas yang cocok di
# `supabase/migrations/**` pada `main`, dan SUDAH dicatat di DECISIONS.md
# (bukan didiamkan diam-diam). Satu slug per baris, `#` untuk komentar.
#
# Menambah baris di sini TANPA entri DECISIONS.md yang menjelaskannya adalah
# bug kelasnya sendiri — allowlist ini bukan tempat menyembunyikan drift baru,
# hanya tempat MENAHAN NOISE dari drift lama yang sudah diverifikasi jinak.

# m6a_section_d — dua baris ledger untuk satu berkas repo
# (20260808020000_m6a_section_d + 20260808000000_m6a_section_d), sisa apply
# ganda LAMA (bukan skema berlebih). Diverifikasi + dicatat DECISIONS.md
# 2026-09-06 (audit drift SCR-UI-1) sebelum A2/B3 ada. CATATAN: gerbang ini
# mencocokkan per-SLUG UNIK, jadi dua baris live yang sama-sama melucuti ke
# "m6a_section_d" TIDAK PERNAH muncul sebagai EXTRA (repo juga punya slug itu)
# — baris di bawah ini nol efek pada perilaku gerbang, murni riwayat.
m6a_section_d

# hotfix_check_login_rate_limit_compat — ledger live TIDAK PERNAH lupa apply
# lama walau fungsinya sudah di-DROP belakangan: baris ini menandai kapan
# shim `check_login_rate_limit(text,int,int)` sempat diterapkan LANGSUNG ke
# live (2026-09-14), sebelum `20261113010000_drop_check_login_rate_limit_shim.sql`
# menghapus fungsinya untuk selamanya (diverifikasi live: nol baris `pg_proc`
# bernama itu). Baris LEDGER tetap ada karena ledger append-only — bukan
# fungsi yang masih hidup. Diverifikasi + ditutup DECISIONS.md 2026-09-17
# baris "DRIFT-SISA-CHECK-LOGIN-RATE-LIMIT" ("🟢 DITUTUP 2026-09-17/18 ...
# Repo↔live sekarang paritas fungsi PERSIS (nol sisa drift)") — nol tindakan
# lanjutan diperlukan, murni riwayat ledger seperti m6a_section_d di atas.
hotfix_check_login_rate_limit_compat
