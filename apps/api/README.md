# CDPS API

API-only Next.js application (TypeScript) serving CDPS backend routes. Do not place UI/React components here; all routes are server-side API handlers.

Refer to `docs/SUPABASE_MIGRATION_PLAN.md` §2.3 for architectural overview and integration patterns.

**Workspace tooling & monorepo config coming in next phase.**
