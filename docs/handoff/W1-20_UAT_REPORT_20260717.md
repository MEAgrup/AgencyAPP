# W1-20 — Laporan UAT teknis (2026-07-17)

**Hasil: PASS 32/32 langkah teknis** (runbook `W1-20_UAT_RUNBOOK.md` langkah 1–17,
dieksekusi via API dengan aktor riil + fixture; skrip repeatable:
`backend/uat/w120_walk.py`, jalankan setelah boot order README import_samples §UAT).

## Lingkungan
Stack dev container: MariaDB lokal, `cmd/mockhris` + `cmd/cdps` dengan
`employees_uat.csv` (33 riil + 2 fixture Director O26 + 2 fixture Finance O33),
`rolemapseed` (25 mapping + 3 layered), `mslseed` 32 layanan (aktor Head of Sales riil
`2101180004`). Branch `claude/wave-2-uat-mock-hris-ebkdyq` (integrasi Wave 2 sudah masuk).

## Aktor
| Peran runbook | Akun | Riil/fixture |
|---|---|---|
| Sales Staff | SAFFIRA MARWAH DESINTA (`2404160367`) | riil (email uppercase — vektor case lolos) |
| Sales Head | CUCU NURHAYATI (`2101180004`) | riil |
| Finance Staff / Head | `UATFIN0001` / `UATFIN0002` | **fixture — lihat Open O33** |
| Account Staff (CRO/AM) | SYIFA NUR ALYA PUTRI (`2412090425`) | riil |
| Account Lead | YULIANTI HANDAYANI (`2305100275`, Head of Account) | riil |
| OD (layered) | OKFA RENDI WIRATAMA (`2409230432`) | riil |
| Director | `UATDIR0001` | **fixture — lihat Open O26** |

## Bukti kunci per langkah (status/pesan BI persis)
- **[2]** Login 8 akun lintas peran OK (termasuk fixture); role resolution benar.
- **[3]** `LEAD-202607-NNNN`/`PRSP-` terbit hanya setelah field wajib; kosong →
  `[data tidak lengkap, silahkan lengkapi semua pertanyaan wajib!]`; re-registrasi nomor
  sama → `[anda sudah memiliki prospek aktif untuk lead ini]` (dedup v2).
- **[4]** `New Lead → Contacted`; transisi ilegal → 409 `[transisi status tidak diizinkan]`.
- **[5]** Quote-preview server-side: Estimasi Nilai `Rp. 12.000.000,00` (Store Management
  (Paket) ×2 dari MSL riil), komisi `Rp. 0,00` (SAH per O24); Qualified Form → `Qualified`.
- **[6]** Negosiasi custom term → approval: staff sendiri DITOLAK 403, Sales Head approve OK.
- **[7]** Closing: alokasi 90% → `[total alokasi sales harus 100%]`; alokasi benar (70/30)
  → `CLI-202607-0002` + `TRX-202607-0002` + `SVC-` + 3 `INST-` **atomik**.
- **[8]** Visibility: Account 404 pra-verifikasi; Sales PIC & OD melihat provenance penuh.
- **[9]** Payment Intent `[Termin]`: non-primary DITOLAK 403; Primary Sales PIC OK;
  TRX muncul di antrean Finance `[Menunggu Verifikasi]`.
- **[10]** 3 INST lahir `[Belum Jatuh Tempo]`, `Rp. 4.000.000,00` masing-masing.
- **[11]** Verifikasi INST-1 → TRX `[Terverifikasi - Sebagian]`, Amount Verified/Outstanding
  terformat IDR; **gate rilis fire-once menembak** (`released_to_account_at` + audit
  `released_to_account`); Account **Lead** melihat klien released.
  ⚠ Catatan semantik: Account **staff** (AM) tetap 404 — **benar per M6 §3** (visibilitas AM
  dipersempit ke klien assigned via `assigned_am_id`, migrasi 0020); runbook lama ditulis
  pra-M6. Langkah runbook diperbarui di walk script (11b/11c).
- **[12]** Over-verify → 422 `[jumlah melebihi total transaksi, periksa kembali]`, tanpa jejak.
- **[13]** Verifikasi penutup tanpa kontrak → 422 `[kontrak belum diupload, lengkapi sebelum
  verifikasi penuh]`; setelah kontrak → TRX `[Lunas]` saat semua INST terverifikasi.
- **[14]** Reminder scan + dashboard jalan (deal ini tanpa overdue — jalur `[Jatuh Tempo]`/H-3
  tercakup unit test W1-17; simulasi overdue riil menyusul di UAT staging bila diminta).
- **[16]** OD menelusuri audit chain TRX (closing → verifikasi → `[Lunas]`) — immutable.
- **[17]** Recompute: Amount Verified `Rp. 12.000.000,00` == nilai transaksi.

## Tidak dijalankan (dengan alasan)
- **[15]** jalur sengketa `[Bermasalah]` — opsional per runbook; tercakup unit test W1-18.
- **[18]** **go/no-go = keputusan manusia (Nerissa/Yohan + head dev)** — laporan ini bahannya.
- Spot-check komisi manual Sales Head (AC langkah 5) — manusia; nilai tersaji di atas.

## Temuan
1. **O33 (BARU, blocking produksi M5): roster HR riil (39) tidak memiliki satu pun karyawan
   divisi FINANCE** — flow verifikasi pembayaran/reminder/skema tidak punya aktor riil.
   UAT memakai fixture `UATFIN0001/0002` + mapping UAT `FINANCE→Finance`. Butuh keputusan
   Yohan: siapa aktor Finance riil (mapping HRGA? karyawan finance di luar sheet HR? Director?).
2. Visibilitas Account staff pasca-rilis berubah oleh M6 (assigned-AM) — bukan bug;
   runbook W1-20 langkah 8/11 perlu kalimat "Account Lead" (bukan "Account").
3. Artefak UAT di DB dev bersifat ephemeral (2 deal UAT; 1 attempt tertinggal di
   Negotiation-Approved dari run pertama yang gagal di closing — tidak mengganggu).
