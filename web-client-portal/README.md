# web-client-portal

External Client Portal (React/Next). SEPARATE auth realm from web-internal,
strict allow-list data layer (Phase 0 v2 §11) — never a permission-trimmed
internal view. Built LAST (Wave 3), after the detailed security spec.
