/**
 * @cdps/core adsscanner — advertiser decision-support tools, one namespace
 * per platform (mirrors the `../report` layout, just platform-first from the
 * start since this family started multi-platform in the plan, see
 * `docs/plan/PLAN_KONSOLIDASI_ALAT_ADVERTISER.md` §7).
 */
export * as tiktok from './tiktok';
