/**
 * @cdps/db — Postgres client & generated types for CDPS
 *
 * Dependencies to be added during implementation:
 * - postgres.js: Postgres client with query builder
 * - drizzle-orm: Type-safe ORM layer
 *
 * Provides:
 * - Client initialization (Supabase Postgres connection via pooler)
 * - Generated types from schema (Drizzle TypeGen)
 * - Query utilities for common patterns (audit insert, entity select, etc.)
 *
 * Reference: SUPABASE_MIGRATION_TECH_APPENDIX.md (Lampiran Teknis §E.3, database pooler config)
 */

// Placeholder exports — implementation follows per phase
export {};
